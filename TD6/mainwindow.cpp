#include "mainwindow.h"
#include "ui_mainwindow.h"

/*
MainWindow::MainWindow(model::Board& chessBoard, QWidget* parent): QMainWindow(parent),
    uiChessBoard(chessBoard)
{
    ui.setupUi(this);

    bool connectMouse = connect(this, SIGNAL(clickedSignal(int, int)), this, SLOT(squareClickedPosition(int, int)));
    Q_ASSERT(connectMouse);
}

MainWindow::~MainWindow()
{
    //delete ui;
}
*/

